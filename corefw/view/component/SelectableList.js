// Generated by CoffeeScript 1.8.0
Ext.define('Corefw.view.component.SelectableList', {
  extend: 'Ext.view.View',
  alias: 'widget.selectableList',
  requires: ['Corefw.util.Formatter', 'Corefw.store.TextLookup'],
  itemSelector: 'div.iemFisDtV',
  emptyText: '',
  minHeight: 120,
  maxHeight: 180,
  minWidth: 230,
  autoScroll: true,
  checkboxes: [],
  listeners: {
    refresh: function(me, eOpts) {
      var i, rc, renderSelector;
      me.checkboxes = [];
      renderSelector = Ext.query('div.iemFisDtV', me.el.dom);
      for (i in renderSelector) {
        rc = this.store.getAt(i).data;
        me.checkboxes.push(Ext.create('Ext.form.field.Checkbox', {
          boxLabel: me.labelRender(rc, me),
          inputValue: rc.text,
          checked: me.checked(rc, me),
          renderTo: renderSelector[i]
        }));
      }
    }
  },
  initComponent: function() {
    this.store = Ext.create('Corefw.store.TextLookup');
    this.tpl = new Ext.XTemplate('<tpl for=".">', '<div class="iemFisDtV">', '</div>', '</tpl>', {
      compiled: true
    }, {
      listStore: this.store
    });
    this.callParent();
  },
  labelRender: function(operand, me) {
    return operand.text;
  },
  checked: function(operand, me) {
    return false;
  }
});
