// Generated by CoffeeScript 1.8.0
Ext.define('Corefw.view.component.MenuWin', {
  extend: 'Ext.menu.Menu',
  componentCls: 'menuWin',
  toolbarAlignment: null,
  itemHeight: null,
  itemVSpacing: null,
  initComponent: function(cfg) {
    var index, item, _i, _len, _ref;
    _ref = this.items;
    for (index = _i = 0, _len = _ref.length; _i < _len; index = ++_i) {
      item = _ref[index];
      if (this.itemHeight && !item.height) {
        item.height = this.itemHeight;
      }
      if (this.itemVSpacing) {
        if (index) {
          item.margin = '' + this.itemVSpacing + ' 0 0 0';
        } else {
          item.margin = '0 0 0 0';
        }
      }
    }
    if (this.toolbarAlignment === 'right') {
      if (this.dockedItems && this.dockedItems.length) {
        this.dockedItems[0].items.unshift('->');
      }
      if (this.bbar && this.bbar.length) {
        this.bbar.unshift('->');
      }
    }
    this.callParent(arguments);
  }
});
