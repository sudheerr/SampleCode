// Generated by CoffeeScript 1.8.0
Ext.define('Corefw.data.reader.Json', {
  extend: 'Ext.data.reader.Json',
  xtype: 'corejsonreader',
  getResponseData: function(response) {
    var data, err, jsonResolver, ret;
    try {
      data = Ext.decode(response.responseText);
      if (jsonResolver = this.jsonResolver) {
        data = jsonResolver.call(this, data);
      }
      return this.readRecords(data);
    } catch (_error) {
      err = _error;
      ret = new Ext.data.ResultSet({
        total: 0,
        count: 0,
        records: [],
        success: false,
        message: err.message
      });
      this.fireEvent('exception', this, response, ret);
      return Ext.Logger.warn('Unable to parse the JSON returned by the server');
    }
  }
});
